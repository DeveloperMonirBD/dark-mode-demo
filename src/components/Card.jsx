export default function Card() {
    return (
        <div className="max-w-md p-6 rounded-xl shadow-lg bg-white dark:bg-gray-800">
            <h2 className="text-2xl font-bold mb-3">Welcome</h2>

            <p>This card automatically changes when dark mode changes.</p>
        </div>
    );
}
